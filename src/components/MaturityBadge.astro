---
import { MATURITY_LEVELS, type MaturityLevel } from '../consts';

interface Props {
  level: MaturityLevel;
}

const { level } = Astro.props;
const info = MATURITY_LEVELS[level];
---

<span class="badge" data-maturity={level} title={info.description}>
  {info.label}
</span>
